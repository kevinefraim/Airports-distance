import { Typography } from "@mui/material";

const Header = () => {
  return (
    <header className="bg-slate-100 flex h-20 justify-center items-center">
      <Typography variant="h3">Airports - Distance</Typography>
    </header>
  );
};

export default Header;
