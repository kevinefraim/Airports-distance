import React from "react";
import { Typography } from "@mui/material";

const Header = () => {
  return (
    <header className="bg-gray-200 flex h-20 justify-center items-center">
      <Typography variant="h2">Airports - distance</Typography>
    </header>
  );
};

export default Header;
