import MainContainer from "./MainContainer";

const Home = () => {
  return (
    <main className="h-[60%] flex items-center justify-center">
      <MainContainer />
    </main>
  );
};

export default Home;
